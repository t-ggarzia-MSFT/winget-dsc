<Page  
    x:Class="dscui.Views.ValidationListPage"  
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"  
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"  
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"  
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:CommunityToolkit.WinUI.Converters"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:ui="using:CommunityToolkit.WinUI"
    xmlns:pages="using:dscui.Views"
    xmlns:vms="using:dscui.ViewModels"
    xmlns:i="using:Microsoft.Xaml.Interactivity"
    xmlns:ic="using:Microsoft.Xaml.Interactions.Core"
    mc:Ignorable="d">
    <TabView
        TabItemsSource="{x:Bind ViewModel.ValidationViewModels}"
        SelectedIndex="{x:Bind ViewModel.CurrentTabIndex, Mode=TwoWay}"
        TabCloseRequested="CloseValidationTab">
        <i:Interaction.Behaviors>
            <ic:EventTriggerBehavior EventName="AddTabButtonClick">
                <ic:InvokeCommandAction Command="{x:Bind ViewModel.NewTabAddedCommand}" />
            </ic:EventTriggerBehavior>
        </i:Interaction.Behaviors>
        <TabView.TabItemTemplate>
            <DataTemplate>
                <TabViewItem Header="{Binding TabHeader}">
                    <TabViewItem.ContentTemplate>
                        <DataTemplate>
                            <pages:ValidationPage/>
                        </DataTemplate>
                    </TabViewItem.ContentTemplate>
                </TabViewItem>
            </DataTemplate>
        </TabView.TabItemTemplate>
    </TabView>

</Page>
