<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="dscui.Views.ConfigurationFlow.SelectFilePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:dscui.Views.ConfigurationFlow"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid x:Name="ContentArea">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <StackPanel VerticalAlignment="Center" Width="400" Spacing="16">
            <Grid Height="200">
                <Rectangle StrokeDashArray="4,2"
                       Stroke="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                       RadiusX="4"
                       RadiusY="4"
                       StrokeThickness="2"/>
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Style="{ThemeResource SubtitleTextBlockStyle}" Padding="20">Import configuration file</TextBlock>
                    <FontIcon Foreground="{ThemeResource TextFillColorSecondaryBrush}" Glyph="&#xE8FF;" FontSize="60" />
                </StackPanel>
            </Grid>

            <StackPanel Spacing="16">
                <TextBlock HorizontalAlignment="Center">No file selected</TextBlock>
                <Button HorizontalAlignment="Center" Style="{ThemeResource AccentButtonStyle}">Browse</Button>
            </StackPanel>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="8" HorizontalAlignment="Right">
            <HyperlinkButton Content="Cancel" MinWidth="120" />
            <Button Content="Previous" MinWidth="120" />
            <Button Content="Next" Style="{ThemeResource AccentButtonStyle}" MinWidth="120" />
        </StackPanel>
    </Grid>
</Page>
